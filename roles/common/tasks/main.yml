---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600
  tags: common

- name: Install required packages
  apt:
    name:
      - git
      - python3-pip
      - curl
    state: present
  tags: common